{{outlet}}

<div class="row">
  <div class="medium-12 columns">
    <button id="save" class="success" {{action 'edit'}}>Tallenna</button>
    <button id="destroy" {{action 'destroy'}}>Poista tila</button>
  </div>
</div>
